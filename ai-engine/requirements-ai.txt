# ==========================================================
# قطاع الذكاء الاصطناعي (ai-engine)
# تحويل الكلام إلى نص + التلخيص + استخراج بنود العمل
# التثبيت:  pip install -r requirements.txt
# ملاحظة: ثبّت ffmpeg على النظام أولاً (إلزامي لـ Whisper)
# ==========================================================

# --- تحويل الكلام إلى نص (Speech-to-Text) ---
openai-whisper>=20250625      # نموذج Whisper من OpenAI

# --- التعلم العميق (يعمل عليه Whisper والنماذج) ---
# ملاحظة: على ويندوز/بدون كرت رسومات، استخدم نسخة CPU من الموقع الرسمي:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2

# --- معالجة اللغة الطبيعية (تلخيص / تصنيف / استخراج مهام) ---
transformers>=4.44            # نماذج Hugging Face
sentencepiece>=0.2            # مطلوب لبعض نماذج التلخيص العربية
protobuf>=4.25                # اعتمادية لبعض نماذج transformers

# --- معالجة الصوت المساعدة ---
librosa>=0.10                 # تحليل ومعالجة الصوت (اختياري)
ffmpeg-python>=0.2            # واجهة بايثون لـ ffmpeg
numpy>=1.26
